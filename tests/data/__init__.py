"""Test-data package.

Each module corresponds to a test domain and contains only pure-data
constants — no logic, no imports of production code.

Modules
-------
    login_data      EXPECTED_ERROR, credentials helpers
    cart_data       product lists for cart scenarios
    checkout_data   shopper info, product, confirmation string
    sort_data       sort option keys
    posts_data      post IDs, payloads for CRUD scenarios
"""
